type Query {
  tasks: [Task!]!
  taskStatuses: [String]!
}

type Mutation {
  createTask(
    title: String!
    description: String!
  ): Task!
  updateTask(
    id: String!
    title: String!
    description: String!
  ): Task!
  deleteTaks(
    id: String!
  ):Task!
  setTaskStatus(
    id: String!
    status: String!
  ):Task!
}

type Task {
  id: ID!
  title: String!
  description: String!
  createdAt: String!
  updatedAt: String!
  status: String!
}